syntax = "proto3";

package go.micro.srv.post;

service Comment {
    rpc GetComments(ReqComment) returns (RspComments) {}
}

message ReqComment {
    int64 id = 1;
}

message RspComments {
    repeated CommentDto comments = 1;
}

message CommentDto {
    string content = 1;
}